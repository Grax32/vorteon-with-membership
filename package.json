{
  "name": "vorteon-with-membership",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-jade": "cross-env ASPNETCORE_URLS=http://localhost:4540 dotnet run --project ./src/vorteon.csproj --no-launch-profile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3"
  }
}
